import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Link a device' };

export default function LinkLayout({ children }: { children: React.ReactNode }) {
  return children;
}
